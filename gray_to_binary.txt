gray = input("Enter gray code: ")
binary = gray[0]

for i in range(1, len(gray)):
    prev = int(binary[i-1])
    g = int(gray[i])

    
    if prev == g:
        binary += "0"
    else:
        binary += "1"

print("Binary Number:", binary)
